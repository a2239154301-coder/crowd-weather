# 優先01 技術検証の結果（2026-08-10）

UIを作る前に「Vision + Structured Outputs で会場写真から Venue JSON が取れるか」を確認した。

## 結論: 通る。実装に進んでよい。

| モデル | 秒 | in/out tok | zones | buildings | 品質チェック |
|---|---|---|---|---|---|
| google/gemini-2.5-pro | 62.1 | 2421 / 7155 | 10 | 11 | ✅ 全項目パス |
| openai/gpt-4o | 6.9 | 2170 / 394 | 3 | 3 | ✅ パスだが粒度が粗い |

品質チェックの内容: 座標が 0-1000 x 0-700 の範囲内か / 頂点が3個以上か / 面積が極小でないか / 建物の高さが正の数か

## 座標の妥当性（写真と照合）

正しく取れていたもの:
- メインステージ 上中央 y180-250 → 写真の中央奥のステージと一致
- メイン観客エリア x200-680 → ステージ前の密集エリアと一致
- PAタワー 下中央 → 中央のやぐら状構造物と一致
- 仮設トイレ群 中左 x100-130 → 左側の白い仮設トイレ列と一致
- 東側ビル（駐車場）右 / 北側ビル群 上 y0-150 → 背景の建物群と一致
- 飲食・物販通路 下右 → 右側のキッチンカー・パラソル群と一致

ずれていたもの:
- 「サイド観客エリア」(x120-280) と「トイレ待機列」(x100-180) が左側で重複
- 実際は左側に長い待機列があるが、テント待機列が下端 y600-700 だけになっている

→ **位置の精度は完璧ではない。人が補正するUIとセットにする前提は正しい。**

## モデル選定

`google/gemini-2.5-pro` を第一候補にする。62秒かかるが:
- 会場ごとに1回きりの処理なので、待ち時間は許容できる
- gpt-4o は6.9秒と速いが zones 3件と粒度が粗く、実用にならない
- 出力7155トークンを要するため max_tokens は 8000 必要（4000では途中で切れる）

## 判明した制約

1. 写真からは **縮尺・方位・建物の高さ** が取れない。AI自身も notes で
   「方位、建物の正確な高さ、会場の正確な縮尺は写真から判断できないため、
   人が補正する必要があります」と明示的に返してきた
2. 座標は 1000x700 に正規化させる指示が有効に働いた（範囲外の座標はゼロ件）
3. `strict: true` の json_schema が Gemini でも機能する

---

# 追試: 本番60秒制限への対応（2026-08-11）

gemini-2.5-pro は Vercel Hobby の60秒上限に収まらないため、モデルを再選定した。
条件: ゾーン上限8件の指示つき・temperature 0・json_schema strict。

| モデル | 画像 | 秒 | zones | 判定 |
|---|---|---|---|---|
| openai/gpt-4.1 | 1024px | **8.1** | **8** 確信度high | ✅ **採用**。座標も検証済み参照とおおむね一致 |
| openai/gpt-4.1 | 768px | 8.3〜11.7 | 5〜6 | 1024pxのほうが粒度が出る。時間は同じ |
| openai/gpt-4o | 768px | 6.9〜8.6 | 3〜4 | 粗い。第一フォールバック |
| google/gemini-2.5-flash-lite | 768px | 9.0 | 8 | 速いが座標が雑（全面を覆うゾーンを作る）。英語で返す。最終フォールバック |
| google/gemini-2.5-pro | 768px | 54.1〜77.4 | 8 | thinking 4,653〜5,665 tok。60秒に収まらず不採用 |
| google/gemini-2.5-flash | 768px | 52.6〜58.1 | 8 | thinking 8,072 tok。不採用 |
| qwen/qwen3-vl-235b-a22b-instruct | 768px | 57.5 | 8 | 遅い。不採用 |

## 潰した仮説2つ

1. **「画像を縮小すれば速くなる」は誤りだった。** 1672px→768pxで入力トークンは
   2421→2287とほぼ不変。時間の支配項はモデルの thinking と出力生成であって画像サイズではない。
   （縮小自体はトークン原価と転送量を減らすので、長辺1024pxへの縮小は継続する）
2. **OrcaRouter の reasoning effort 制御はこのデプロイでは使えない。**
   公式docsの `reasoning_effort` フィールドは low / minimal とも reasoning トークン数が
   全く変わらず（8,072 tokで完全一致＝無効）、`-low` サフィックスは
   `model_not_found` (503) が返る。docsと実環境の乖離は要注意（deckのモデルID問題と同根）。

## 結果（ローカルUI経由・エンドツーエンド）

17.2秒 / gpt-4.1 / fallbackLevel 0 / ゾーン7件・構造物4件・確信度high。
40秒以内・6〜8件の目標をどちらも満たす。
