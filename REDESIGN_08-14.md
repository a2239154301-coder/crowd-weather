# 実装指示書 — 明色化と機能整理（馬場氏レビュー13項目）

**起案 2026-08-14 / 起案元セッション: `dev\08_CrowdWeather`（仕様正本側）／実装: このリポジトリのセッション**

出典は馬場貴光氏のPDF `スタッフ用画面などの背景色の変更.pdf`（2026-08-13・2ページ）。
13項目の指示に対し、コードを実測して実行計画に落としたもの。**土田氏の承認済み**。

> **行番号について**: 本書の行番号は `f51a2ef`（第4弾）時点の実測値。作業が進むとずれるので、
> **必ず併記のコード片で検索して特定すること**。行番号だけを信用しない。

### 作業の順番（Stage名と節番号の対応）

| 順 | Stage | 節 | 内容 | 目視なしの安全度 |
|---|---|---|---|---|
| 0 | — | **§1** | トークン定義（`day-theme.ts` に4つ追加）。最初にやる | — |
| 1 | **Stage 1** | **§2** | 先に減らす（削除・整理6件） | 安全 |
| 2 | **Stage 2** | **§3** | 明色化 | 安全（値の差し替えでレイアウトは動かない） |
| 3 | **Stage 3** | **§4** | 仕上げ（§4-1 は安全 / **§4-2 は目視必須**） | §4-2 のみ要目視 |

**この順序を崩さないこと。** 塗る面積を減らしてから明色化する。特に §2-6（DataView の切り出し）は
§3-3（審査モードの明色化）の前提なので飛ばせない。

---

## 0. 前提と制約

### 変更してよい範囲
画面のレイアウト・情報の配置・タブ構成・どの機能をどの画面に出すか・視覚的な優先順位・
新規の集約/分割コンポーネント。

### 変更してはいけないもの
- **`lib/forecast/{model,wbgt,venue,scales,route,tent,solar}.ts`** — 予報計算の核。
  **`scales.ts` に含まれる `INK` トークンも触らない**。新しい色トークンは `lib/ui/day-theme.ts` 側に足す
- `components/original*/` — 馬場氏原本の比較ページ
- **段階色（`DENSITY_BANDS` / `WBGT_BANDS` / `TIME_BANDS`）の色値** — ΔE検証済み資産。
  使い方（塗り・点・帯にだけ使い、明色地の文字は必ず墨色で書く）は `day-theme.ts` 冒頭の既存規約に従う
- `lib/ops/staffing.ts` の `postsFor` 採番ロジック
- `/api/dispatch` への入口は人間承認UIだけ、という設計
- **当日モード（状況/調整/配置/スタッフ）と来場者モードの配色** — 既に明色。触らない

### 各Stage終了時に必ず緑にすること
```bash
npm run typecheck && npm test && npm run build
```
テストは **273件**（`f51a2ef` 時点）。**1件も減らさないこと**。

---

## 1. トークン定義（Stage 2 の土台・最初にやる）

### 1-1. `lib/ui/day-theme.ts` に4つ追加

`INK` にあって `DAY` に無いものを補う。**既存キーの値は変えない**（本番稼働中・コントラスト実測済み）。

```ts
  /**
   * INK.raised の明色対応。白いカードの中の一段沈んだ面
   * （明色では「浮く」ではなく「沈む」で表す。page < surface の並びに対し raised は最も濃い）。
   * surface(#FFFFFF) 上で 1.21:1。**page(#EEF1F7) の上に直接置くと 1.07:1 でほぼ見えない**ので、
   * その場合は `border: 1px solid DAY.line` を併用すること。
   */
  raised: "#E3EAF6",
  /**
   * INK.hairline の明色対応。line より弱い区切り。
   * 暗色版の段差比（hairline は line の約40%の強さ）に合わせて算出:
   * 暗色 line 1.360 / hairline 1.143 → 明色 line 1.519 なので hairline は 1.23 前後が対応する。
   */
  hairline: "#E1E8F3",
  /** 選択中・操作の色（馬場氏「水色〜ブルー」）。明色地で文字に使える濃さが要る */
  accent: "#0B5C8C",
  /** accent の淡い塗り。チップ・選択行の背景に */
  accentSoft: "#E6F2FE",
```

さらに **ナビ帯専用の青**を別エクスポートで足す（暗色帯の上に置くので `DAY.accent` では暗すぎる）:

```ts
/**
 * 上部ナビ（ダークネイビー据置）の選択中タブの青。
 * 明色地ではなく INK.surface(#121826) の帯の上に置くため DAY.accent より明るい。
 * INK は凍結ファイル scales.ts にあるので、こちら側に持つ。
 */
export const NAV_ACCENT = "#2E7FC2";
```

### 1-2. コントラスト（WCAG式で計算検証済み・2026-08-14・再計算不要）

| 前景 | 背景 | 比 | 判定 | 用途 |
|---|---|---|---|---|
| `#0B5C8C` | `#FFFFFF` | **7.18:1** | AA本文 | accent を白カード上の文字・アイコンに |
| `#0B5C8C` | `#EEF1F7` | **6.35:1** | AA本文 | accent を page 上の文字に |
| `#FFFFFF` | `#0B5C8C` | **7.18:1** | AA本文 | accent 塗りの上の白文字 |
| `#FFFFFF` | `#2E7FC2` | **4.26:1** | 大文字・UI | ナビの選択中タブの白文字。**15px/700 なので大文字扱いで合格**。本文には使わない |
| `#2E7FC2` | `#121826` | **4.16:1** | UI部品 | ナビ帯の上でタブが面として立つか（3:1要件を超過） |
| `#0B111F` | `#E3EAF6` | **15.59:1** | AA本文 | raised 面の本文 |
| `#0B111F` | `#EEF1F7` | 16.66:1 | AA本文 | （既存・参考） |
| `#4A5670` | `#FFFFFF` | 7.35:1 | AA本文 | textDim（既存・参考） |
| `#5A6685` | `#FFFFFF` | 5.72:1 | AA本文 | textFaint（既存・参考） |

**旧色が明色地で不合格になることの定量的な裏付け**（§3-4 の根拠）:

| 色 | 白地コントラスト | 判定 |
|---|---|---|
| `#FDE047`（二次資料） | **1.32:1** | **判読不能** |
| `#22C55E`（一次確認済み） | 2.28:1 | 不合格 |
| `#FB923C`（推定） | 2.26:1 | 不合格 |

### 1-3. `INK.` → `DAY.` 対応表（機械的置換の基準）

| INK | 値 | → DAY | 値 |
|---|---|---|---|
| `page` | `#0A0E17` | `page` | `#EEF1F7` |
| `surface` | `#121826` | `surface` | `#FFFFFF` |
| `raised` | `#182032` | `raised` | `#E3EAF6`（新） |
| `line` | `#243149` | `line` | `#C9D2E4` |
| `hairline` | `#1B2437` | `hairline` | `#E1E8F3`（新） |
| `text` | `#E8EEF9` | `text` | `#0B111F` |
| `textDim` | `#A9B6D0` | `textDim` | `#4A5670` |
| `textFaint` | `#7A88A6` | `textFaint` | `#5A6685` |
| `accent` | `#7DD3FC` | `accent` | `#0B5C8C`（新） |

**機械的に置換できない3パターン（手で見ること）**

1. **反転チップ** — `background: X ? INK.text : "transparent"` / `color: X ? INK.page : INK.textDim` の組。
   置換すると「濃紺の塗り＋ほぼ白の文字」になり明色側でも成立する（`staff-board.tsx` に同じ形の前例あり）。
   そのまま置換してよいが、**ナビ帯だけは Stage 3 で青に変えるので触らない**
2. **`${color}1A` のアルファ連結** — 暗色地の10%と明色地の10%では見え方が違う。
   `1A`(10%) では白地で消えるので **`22`〜`2E`(13〜18%) へ上げる**。
   **明色化するファイルのものだけが対象**:
   `ingest-panel.tsx:500-501` / `ingest-panel.tsx:241` / `ops-console.tsx:1190-1191` /
   `zone-timeline.tsx:156`。
   **触らないもの**: `heat-map.tsx:208`（暗色据置のファイル。`55` のままでよい）／
   `visitor-route.tsx:760-761`（既に明色画面で成立している）
3. **段階色・パステルを文字色に使っている箇所** — 明色地で読めなくなる。**§3-4 参照**。

   > **§3-4 に個別に挙げた以外にも必ずある。** 明色化する各ファイルで**生の `#hex` を grep し、
   > `color:` として使われているものを全部拾って墨色系へ振り直すこと**（`background` や
   > ドットの塗りに使われているものは対象外）。
   >
   > 既知の例: 3系統ブリーフィングの見出し色（`ops-console.tsx:900-945` の `.map` の配列リテラル）。
   > **暗色地では良好なのに白地で壊れる**という、明色化で最も見落としやすい型:
   >
   > | 色 | 用途 | 暗色地(#121826) | **白地** |
   > |---|---|---|---|
   > | `#C4B5FD` | 警備責任者向け | 9.60:1 | **1.85:1** |
   > | `#7DD3FC` | 現場スタッフ向け | 10.63:1 | **1.67:1** |
   > | `#86EFAC` | 来場者向け | 12.63:1 | 1.40:1（§2-3 で行ごと消えるので対象外） |
   >
   > ラベル文字は墨色（`DAY.text` / `DAY.textDim`）にし、**色は左の小さなドットへ移す**。
   > 系統の区別は色ではなく語（「警備責任者向け」「現場スタッフ向け」）が既に担っている

---

## 2. Stage 1 — 先に減らす

**塗る面積を減らしてから明色化する。この順序を崩さないこと。**

### 2-1. 作業手順ステッパーの削除（項目3）

`components/app-shell.tsx`:
- `STEPS` 定義（`const STEPS: { n: number; label: string; hint: string; goto?: OrganizerView }[]` で検索・:60-66）を削除
- `CURRENT_STEPS` 定義（:68-76）を削除
- 描画ブロック `<ol className="cw-steps"` から対応する `</ol>` まで（:278-340）を削除
- インライン `<style>` 内の `.cw-steps { display: none !important; }`（:159）を削除

**背景**: タブと情報が重複しており、`STEPS` の2番目と3番目は `goto` が両方 `"ops"` で
別ステップに見えて遷移先が同一だった。

### 2-2. リスク予報レイヤーを `/heatmap` へ集約（項目5）

**判断の根拠**（馬場氏の問い「ほかに影響しないなら不要では？」への回答）:
- `zoneRisks()` は「各ゾーンが最初に危険（severity 3）に入る時刻・その時刻の予報値・要因」を返す純関数
- **地図のriskレイヤーと「危険の到来順」リストは同一データ**。地図は空間で、リストは時間順で描いている
- **退避先は既に存在する** — `/heatmap` 全画面ページが `layer="risk"` の同じ地図を持つ
  （`heatmap-console.tsx:122, 333`・既定ビュー）。予報コンソールから消しても情報は失われない

**`components/ops-console.tsx`**
- レイヤートグル（`options={[["risk", "リスク予報"], ["crowd", "混雑"], ["heat", "暑熱・日陰"]]}` で検索・:620-628）
  から `["risk", "リスク予報"]` を削除
- 初期値 `useState<MapLayer>("risk")`（:38）を `"crowd"` へ
- 解説段落（:660-678・「色はいまの値ではなく、危険帯に入るまでの残り時間」）→ **§2-5 で審査モードへ移設**
- 凡例（`<Legend layer>` の else 分岐 `[...TIME_BANDS].reverse()`・:1238-1244）から risk 分岐を削除

**`components/venue-map.tsx` は一切触らない。**

> ⚠ **ここを間違えないこと。** `MapLayer` union から `"risk"` を消したくなるが、
> **`/heatmap` が `<VenueMap layer="risk">` を渡しているので型エラーで壊れる**
> （`heatmap-console.tsx:333`）。そして `/heatmap` こそが今回のリスク地図の**退避先**である。
>
> つまりこの作業は「リスク地図を削除する」のではなく「**予報コンソールから出口を1つ減らし、
> `/heatmap` に一本化する**」。`venue-map.tsx` のリスク描画（`risks` memo・`timeBand` 分岐・
> `riskLabel`・ツールチップの「予報」行）は**全て残す**。

**`lib/forecast/risk.ts` も一切触らない。** `riskLabel` を含む全 export が引き続き使用中
（`/heatmap` の地図・当日モード・到来順リスト・PEAKバッジ）。

この変更は **`ops-console.tsx` の中だけで完結する**。差分が小さいのが正しい姿。

**あわせて「会場内／会場外」の切替を廃止する** — ここが馬場氏の言う「機能の混在」の実体。

現状 `zones` は risk レイヤーのときだけ18ゾーン全部、他は scope トグルで10 or 8。
この分岐が時間帯別ストリップ・3カラムスタット・ZONE TIMELINE・**AI 3ボタンの入力**まで波及していた。

`components/ops-console.tsx`:
- `const zones = useMemo(() => (layer === "risk" ? VENUE.zones : zonesFor(scope)), [layer, scope]);`（:115-118）
  → `const zones = VENUE.zones;`（全画面で18ゾーン合算に統一）
- `scopeLabel`（:119）→ 定数 `"会場内＋会場外"` に
- 会場内/外トグル（:597-619）を削除。`scope` state も削除
- `scope === "out"` の会場外解説（:701-720）を削除
- `zonesFor` の import が不要になれば外す（**他ファイルの `zonesFor("in")` は残す**）

> **注意**: `zones` を18ゾーン固定にすると、3カラムスタット・時間帯別ストリップ・ZONE TIMELINE・
> `/api/advice` へ渡す `scope` が全て「会場内＋会場外」になる。これは意図した変更。
> ZONE TIMELINE が10行→18行に増えるので、縦の伸びを確認すること。

### 2-3. 3系統ブリーフィングから来場者向けを外す（項目6）

馬場氏「予報コンソールにはディレクター向け・スタッフ向けの情報のみにする。
現在は来場者向け情報も出てきて混乱する」。

`components/ops-console.tsx`:
- `audiences` 配列（`[["responsible","警備責任者"],["staff","現場スタッフ"],["visitor","来場者"]]` で検索・:210-239）
  から `visitor` を削除
- 表示側の配列（`["visitor","来場者向け","#86EFAC"]` を含む `.map`・:900-945）から該当行を削除
- グリッド `gridTemplateColumns: "1fr 1fr 1fr"`（:903）→ `"1fr 1fr"`

**サーバー側は変更不要**（`lib/ai/prompts.ts:67-70` の visitor プロンプトと
`app/api/advice/route.ts` はそのまま残す。来場者ページへ移すときに再利用する）。

### 2-4. AI 2ボタンを「現場に出すべき指示」に一本化（項目8）

**判断の根拠**（馬場氏「違いは？どちらかでもいいのでは？」への回答）:
- 同一API（`/api/advice`）・同一モデルティア（task="advice"）。**差は `mode` 文字列1個と system prompt だけ**
- 出力表示ブロック（:806-830）と state（`advice` / `adviceLabel`）を共有しており、
  **後から押した方が前の結果を上書きする**。つまり現状も両方は見られない

`components/ops-console.tsx`:
- 「運営判断を聞く」ボタン（:766-783・`askOrca("advice")`）を削除
- `easyStyle` チェックボックス（:796-802）は「現場に出すべき指示」に効くので残す

**`ADVICE_SYSTEM`（`prompts.ts:10-21`）と API の `mode` 分岐（`route.ts:52-57`）はサーバーに残す。**
後で戻せるし、審査で「2系統のプロンプトを持っている」と説明できる。

### 2-5. 説明文の整理（項目7）

製品UIから外し、**必要なものは審査用モードへ移す**（審査項目④⑥の説明はそこが正しい置き場）。

| 箇所 | 検索キー | 扱い |
|---|---|---|
| :739-744 | `2. 現場に出すべき指示` を含む `<div style={{ fontSize: 13, color: INK.textDim` | **削除**。LLM不使用の主張は `trust-panel` に既出 |
| :447-456 | `→ 次は右の` | **削除**（ステッパー削除と整合） |
| :660-678 | `危険帯に入るまでの残り時間` | **審査モードへ移設**（下記） |
| :876-880 | 3系統ブリーフィングの説明 | 1行に圧縮 |
| :960-963 | `AIは質問を条件変更に翻訳するだけ` | 1行に圧縮 |

**:660-678 の移設先**: `components/evidence-panel.tsx` の冒頭か `components/trust-panel.tsx`。
この文はプロダクトの中核主張（「既存サービスが出せるのは"いま混んでいる場所"まで —
ここで出しているのはこれから危なくなる場所」）なので**消さない**。
審査モードは `/heatmap` のリスク地図とセットで説明できる位置にある。

> 実装は全角丸 `〇`（`○` ではない）。grep 時に注意。

### 2-6. DataView の切り出しとデッドコード583行の削除

**Stage 2 の前提。必ずここでやる。**

`components/crowd-weather.tsx`（633行・`@ts-nocheck`）は
**L584-633 の `DataView` 50行だけが審査モードで本番描画**され、残り583行
（`export default function CrowdWeather()` と `PlanDoc`）は完全な未使用。

手順:
1. `components/data-view.tsx` を新設し `DataView` を移設。
   持ち出すのは **`mono` 定数1行 + `Panel` / `Eyebrow` の2ヘルパー + 私有パレット `C` の10キー**
   （`heat` `busy` `line` `faint` `caution` `panel2` `muted` `deep` `cool` `panel`）のみ。
   モデル層に一切触れていないので**自己完結する**
2. `components/crowd-weather.tsx` を削除
3. `components/app-shell.tsx:18` の `import { DataView } from "./crowd-weather";` を
   `from "./data-view"` へ差し替え
4. 移設時に `className="cw-grid"` を
   `style={{ display: "grid", gridTemplateColumns: "repeat(auto-fit,minmax(300px,1fr))", gap: … }}` へ置換

> **既存バグの同時修正になる**: `.cw-grid` のメディアクエリは
> 一度もマウントされない `CrowdWeather` の `<style>` タグ内（旧 :264）にあり、
> **実行時に存在しない**。審査モードは840px以下でも2カラムのまま潰れていた。

---

## 3. Stage 2 — 明色化（項目1）

馬場氏の指定と既存 `DAY` トークンは実質同色なので、**新規トークンは起こさず既存値を使う**。

| 馬場氏指定 | 既存 `DAY` | 差 |
|---|---|---|
| 全体背景 `#F3F6FA` | `page #EEF1F7` | 2%未満 |
| カード `#FFFFFF` | `surface #FFFFFF` | 同一 |
| 通常文字 `#0B1730` | `text #0B111F` | 1%未満 |
| 危険/注意/安全＝赤/オレンジ/緑 | `DENSITY_BANDS` + `DAY.danger` | 既存 |
| 選択中・操作＝水色〜ブルー | `accent #0B5C8C`（新） | — |
| 上部ナビ＝ダークネイビーのまま | `INK.surface` 据置 | — |

### 3-1. シェルの反転（上部ナビだけ暗色を残す）

**`components/app-shell.tsx`**
- 外枠（`<div style={{ minHeight: "100vh", background: INK.page, color: INK.text }}>`・:154）
  → `background: DAY.page, color: DAY.text`
- **モードタブ帯（:191-192 の `background: INK.surface` / `border: 1px solid ${INK.line}`）は
  `INK` のまま**＝馬場氏「上部ナビは今のダークネイビーのまま」
- 帯の中のタブ文字色（`INK.text` / `INK.textDim`・:220-221）も `INK` のまま（Stage 3-1 で青に変える）
- 主催者nav（:249-250）・当日nav（:357-358）は**帯ではなくコンテンツ側**なので `DAY` へ
- judge 導入文（:413 の `INK.textDim`）→ `DAY.textDim`
- フッター（:447 `INK.hairline`、:453 `INK.textFaint`）→ `DAY`

**`app/globals.css`** — ここは `INK` の値を hex で複製している二重管理箇所。
**直し忘れると body だけ暗色に取り残される。**

| 行 | 現在 | → |
|---|---|---|
| 11 | `background: #0a0e17` | `#eef1f7` |
| 12 | `color: #e8eef9` | `#0b111f` |
| `:focus-visible` | `outline: 2px solid #7dd3fc` | `#0b5c8c` |
| `input[type="range"]` track | `background: #243149` | `#c9d2e4` |
| slider thumb | `background: #e8eef9` / `border: 4px solid #0a0e17` / `box-shadow: 0 0 0 1px #243149` | `#0b111f` / `4px solid #ffffff` / `0 0 0 1px #c9d2e4` |

> `/gate` と `/heatmap` は暗色のまま残すが、**両方とも自分で `background: INK.page` を敷いている**
> （`app/gate/page.tsx:98`・`heatmap-console.tsx:166`）ので body を明色にしても壊れない。確認済み。

> 当日モード・来場者モードが自前で敷いている明色ラッパは冗長になるが **そのまま残す**（無害）。

### 3-2. 計画モードの明色化

§1-3 の対応表を適用する。

| ファイル | INK参照 | 備考 |
|---|---|---|
| `components/ops-console.tsx` | 112 | 最大。**Stage 1 の削除後に着手すれば対象が減る** |
| `components/security-plan.tsx` | 30 | **画面JSX（:187-398, :439-446）のみ**。印刷CSSは触らない → §3-5 |
| `components/ingest-panel.tsx` | 29 | |
| `components/hourly-strip.tsx` | 17 | |
| `components/arrival-list.tsx` | 11 | |
| `components/zone-timeline.tsx` | 8 | |
| `components/compare-bar.tsx` | 8 | 全モード共通で最上部に出る |
| `components/plan-output.tsx` | 1 | |
| `components/entry-peak.tsx` | tone prop | 呼び出し側を `tone="ink"` → `"day"`（`ops-console.tsx:1084`） |

**触らない**:
- `components/venue-map.tsx`(35) / `components/heat-map.tsx`(12) — **地図は暗色据置**。
  「明色ページに暗色の地図」は既に本番で成立している（配置タブ・来場者から呼ばれている）
- `app/gate/page.tsx`(10) / `components/heatmap-console.tsx`(30) — 独立ルート。締切内は暗色のまま

### 3-3. 審査モードの明色化

- **`components/trust-panel.tsx`**（INK 31）— **生hex・gradient・rgba・boxShadow すべて0**。
  対応表どおりの差し替えだけで完了する
- **`components/data-view.tsx`**（§2-6 で切り出し済み）— 私有パレット `C` の10キーを `DAY` へ。
  意味色4つ（`heat` `busy` `caution` `cool`）は保持

  > **シェルと必ず同時に行うこと。** DataView の見出し3箇所（旧 `crowd-weather.tsx:603, 613, 622`）は
  > **色指定を持たずシェルから継承している**。シェルだけ明色化すると
  > 「暗色地（`C.panel`）に暗色文字」で判読不能になる。

- **`components/evidence-panel.tsx`**（INK 11 + 生hex 3）— §3-4 参照

### 3-4. 段階色を「文字色」に使っている箇所（明色地で読めなくなる・必ず直す）

`day-theme.ts` 冒頭の既存規約「段階色は塗り・点・帯にだけ使い、明色地の文字は必ず墨色で書く」に従う。

**`components/evidence-panel.tsx`**

| 行 | 現在 | 問題 | → |
|---|---|---|---|
| :59 | 信頼度3段階の**文字色** `#22C55E` / `#FDE047` / `#FB923C` | `#FDE047` は白地で**1.32:1・判読不能**（§1-2に計算値） | 下記へ差し替え |
| :21 | 見出し `#22C55E` | 白地で沈む | `#0F6E56` |
| :31 | 見出し `#FB923C` | 白地で最も落ちる | `#B45309` |

信頼度3段階の置換（いずれも既にコードベースに存在する色・白地コントラスト計算済み）:

| 段階 | 新しい文字色 | 白地コントラスト |
|---|---|---|
| 一次確認済み | `#0F6E56` | **6.20:1** |
| 二次資料 | `#B45309` | **5.02:1** |
| 推定 | `#5A6685`（= `DAY.textFaint`） | **5.72:1** |

**元の段階色は、文字の左に小さな色ドットとして残す**（意味は色＋語の二重で運ぶ）。

**`components/security-plan.tsx`** — 画面側の凡例4色（:326-329 `#38BDF8` 給水 / `#FDE047` 誘導 /
`#22C55E` 救護 / `#C4B5FD` 受付）。**塗り（ドット）なら現状のままでよい。文字色なら墨色へ**。
実装を見て判断すること。

### 3-5. `security-plan.tsx` の印刷／画面の切り分け

**印刷用CSSは触らない。**

| 区分 | 行 | 扱い |
|---|---|---|
| `printPlan()` の `<style>` 文字列 | :145-172（28行） | **対象外**（白紙に出す文書用。hex 13件はここ） |
| `tableHtml()` / `referenceHtml()` / `mapsHtml()` | :400-438 | **対象外** |
| 画面 JSX | :187-398, :439-446 | 明色化する。**INK 30件は全てここ** |

印刷の template literal 内の INK 参照は **0件**。切り分けは既にコード上で成立している。

:136 のコメント「SVGの会場図はそのまま複製する（…暗色のままで読める）」は、
地図を暗色据置にするので**前提は崩れない**。ただし画面が明色になるので文言の見直しはしてよい。

---

## 4. Stage 3 — 仕上げ

### 4-1. 目視なしでも安全（先に実施）

| 項目 | 内容 | 該当 |
|---|---|---|
| **11** | モードタブの文言を「**1 制作向け / 2 現場向け / 3 来場者向け / 4 審査向け**」へ。説明文（`hint`・タプル3番目）は**削除** | `app-shell.tsx:197-203`。`<span style={{ fontSize: 13, opacity: 0.75 }}>{hint}</span>`（:226）も削除。**`MODES`(:46) とラベル配列の二重管理**なので、この機会に1つの配列へまとめると事故が減る |
| **11** | 選択中タブを青系へ | `app-shell.tsx:220-221`。`background: mode === k ? NAV_ACCENT : "transparent"` / `color: mode === k ? "#FFFFFF" : INK.textDim` |
| **12** | 主催者サブタブの順序を **予報コンソール → 会場を読み込む → 計画書出力** へ | `app-shell.tsx:54-58` の `ORGANIZER_TABS` 配列の順序を入れ替えるだけ（`ops` / `ingest` / `output`）。`ORGANIZER_VIEWS`(:47) の順序は URL の許可リストなので変えなくてよい |
| **9** | 見出しを「**ど**こが、いつ、危険になるか」へ | `zone-timeline.tsx:76`。現在は「**ここ**が、いつ、危険になるか」（**読点2つ**）。`ops-console.tsx:1086` のコメントは既に「どこが」なので表記が揃う |
| **9** | 入場ピークパネルの文字サイズを ZONE TIMELINE 側と揃える | `entry-peak.tsx:58`（タイトル 15/700）と **`:62`（サマリー 19/700）**。19px が浮いているので **15** へ。比較対象は `zone-timeline.tsx:76` の 15/600 |
| **10** | 危険の到来順の太字を止める | `arrival-list.tsx:56, 59`。**`fontWeight: 600`（700ではない）→ `400`**。時刻セル(:56)とゾーン名(:59)の2箇所 |
| **13** | 当日モードの LIVE バッジと「現在の状況に更新」を削除 | `live-console.tsx:202-218`（赤い LIVE バッジ）、`:244-265`（ボタンと `実況 … 反映` の結果表示）。`applyLive` ハンドラと `liveAt` state も未使用になれば削除 |

> **項目13と項目4はセット**。当日モードから実況取込を消す一方、計画モードには残す（項目4で位置を変える）。
> 当日モードは常に最新であるべきで手動更新ボタンが不要、計画モードは明示的に条件を取り込む、という分担。

### 4-2. ブラウザで目視できる場合のみ（見ずにやると散らかる）

| 項目 | 内容 | 該当 |
|---|---|---|
| **4** | 「現在の状況に更新」と会場の場所入力を、左レールから**暑熱の横**へ移動 | `ops-console.tsx:369-413`（会場の場所 `<Field label="会場の場所">`）、`:415-446`（実況ボタン）。移動先の「暑熱」がレイヤートグルか3カラムスタットのどちらを指すかは**PDFの図で確認してから確定する**。ボタンの番号プレフィクス「**1.** 現在の状況に更新」はステッパー削除に伴い外す |
| **2** | 各ボタンとスタット（最混雑・最暑熱）にアイコン追加 | **`lucide-react ^1.30.0` が既に `package.json` の依存にある**（現行画面では未使用・凍結モックのみが使用）。新規依存は不要 |

---

## 5. 検証

```bash
npm run typecheck
npm test            # 273件（f51a2ef 時点）。1件も減らさない
npm run build
```

加えて:

1. **残存 `INK.` の検出** — 明色化対象ファイルに `INK.` が残っていないか grep。
   **残っていて正しいのは**: `app-shell.tsx` のナビ帯まわり / `venue-map.tsx` / `heat-map.tsx` /
   `app/gate/page.tsx` / `heatmap-console.tsx`
2. **`globals.css` の取り残し確認** — `#0a0e17` `#e8eef9` `#243149` `#7dd3fc` が残っていないか
2b. **生hexの文字色チェック** — 明色化した各ファイルで `#[0-9A-Fa-f]{3,6}` を grep し、
   `color:` に使われているものを全部コントラスト計算にかける（4.5:1未満は墨色系へ）。
   `background` / ドットの塗りは対象外
3. **コントラスト計算** — 変換後の前景/背景ペアを抽出し本文4.5:1・UI部品3:1を満たすか計算（ピクセル不要）
4. **SSR確認** — `curl` で `/?mode=plan` と `/?mode=judge` を叩き、背景指定が明色になっているか
5. **ブラウザ目視**（ペインが使える場合）— `npm run dev` で4モードをスクリーンショット。
   ローカルは合言葉ゲートが無効（`DEMO_ACCESS_KEY` 未設定）なので素通りで見られる。
   **本番 `crowd-weather-six.vercel.app` はゲート有効**（2026-08-14 確認済み）

**各Stageの終わりで必ずコミットする。** 明色化は差分が大きいので、
戻せる点を作らずに進めると事故ったときに復旧できない。

---

## 6. 馬場さんへ返すべき事実（実装とは別に共有が必要）

| 項目 | 指摘 | 実測した事実 |
|---|---|---|
| **5** | 「リスク予報は何？ ほかに影響しないなら不要では？」 | **影響はある**（会場内＋会場外の合算がこのレイヤー専用で、AI3ボタンの入力まで波及していた）。ただし退避先 `/heatmap` が既にあるので削除できる。あわせて会場内／外の切替も外し、全画面で18ゾーン合算に統一する |
| **8** | 「現場に出すべき指示と運営判断、違いは？」 | **コード上ほぼ同一だった**。同一API・同一モデル・差は `mode` 文字列1個と system prompt のみ。しかも出力欄と state を共有していて**後から押した方が前を消していた**（＝今も両方は見られない）。一本化に実害なし |
| **10** | 「危険の到来順、bold を止めたい」 | 該当箇所は `fontWeight: 600`（**700 ではない**）。600 → 400 にする |
| **9** | 「ここが、いつ危険になるか」 | 実装は「ここが、**いつ、**危険になるか」（**読点2つ**）。「どこが、いつ、危険になるか」に変更する |
| **1** | 背景色の指定 | 指定色は既存の当日モード用パレット（`DAY`）と実質同色だった。新しい色を起こさず、既に本番稼働している値をそのまま使える |

---

## 7. この指示書の出所と、まだ埋まっていない穴

- 出典PDFの**注釈テキストは抽出済み**だが、**同梱のスクリーンショット4点は未確認**
  （起案側セッションでブラウザペインが表示できずキャプチャ不可）。
  **項目4の「暑熱の横」がどこを指すかは、この図を見ないと確定できない**
- 項目3の「〇の部分消す」は、続く「会場を読み込むなどの項目は削除」と項目12（タブ順の入れ替え）の
  整合から**作業手順ステッパーの削除**と解釈した（土田氏確認済み）
- 起案側は `dev\08_CrowdWeather`（仕様正本の置き場）のセッション。
  実装の正本はこのリポジトリなので、**完了後は `dev\08_CrowdWeather\HANDOFF.md` の更新が要る**
